---
import '../styles/global.css';
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "Sobre mi";

const identity = {
  firstName: "Mauri",
  country: "Argentina",
  occupation: "Full Stack WebDeveloper",
  hobbies: ["Caminata con Aquiles", "VideoGames", "River Plate"],
};

const skills = [
  "HTML",
  "CSS",
  "JavaScript",
  "React",
  "Astro",
  "NodeJS",
  "Express",
  "Git",
  "Redacción de documentos",
];
const happy = true;
const finished = true;
const goal = 3;
const skillsColor = "green";
const titleColor = "purple";
const skillFW='bold';
---

    <style define:vars={{ skillsColor, titleColor, skillFW }}>
      h1 {
        color: black;
        background-color: greenyellow;
        font-size: 4rem;
      }    
      .skill {
        color: var(--skillsColor);
        font-weight: var(--skillFW);
      }
    </style>
 
 <BaseLayout pageTitle={pageTitle}>
    <h1 class="rounded-lg px-8">{pageTitle}</h1>
    <h2 class="font-semibold mb-2">... ¡y mi nuevo sitio Astro!</h2>
    <p>
      Estoy trabajando en el tutorial introductorio de Astro. Esta es la segunda
      página de mi sitio web, ¡y es la primera que he construido yo mismo!
    </p>

    <p>
      Este sitio se irá actualizando a medida que vaya completando más partes
      del tutorial, ¡así que no dejes de visitarlo para ver cómo va mi viaje!
    </p>
    <p>He aquí algunos datos sobre mí:</p>
    <ul>
      <li>Me llamo {identity.firstName}.</li>
      <li>Vivo en {identity.country} y trabajo como <strong>{identity.occupation}</strong>.</li>
      <li class="font-semibold my-2">Mis aficiones son:</li>
        <ol>
          {      
            identity.hobbies.map((h)=>        
              <li class=" mx-4 text-sky-800">
                {h}
              </li>
            )
          }
        </ol>
    </ul>
    <p class="m-2">Mis habilidades son:</p>
    <ul>
      {skills.map((skill) => <li class="skill skillColor  ">{skill}</li>)}
    </ul>
    {happy && <p>¡Estoy feliz de aprender Astro!</p>}

    {finished && <p>¡He terminado este tutorial!</p>}

    {
      goal === 3 ? (
        <p>Mi objetivo es terminar en 3 días.</p>
      ) : (
        <p class="mb-2">Mi objetivo no son 3 días.</p>
      )
    }  
</BaseLayout>
